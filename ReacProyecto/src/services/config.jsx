/**
 * @file config.jsx
 * @description Archivo de configuración centralizado para el servicio de red.
 * Contiene las constantes globales utilizadas en las peticiones a la API.
 */

/** 
 * URL base para todas las peticiones a la API del servidor json-server.
 * @type {string} 
 */
export const BASE_URL = "http://localhost:3005";
